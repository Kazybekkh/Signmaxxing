{
  "name": "signmaxxing-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --experimental-strip-types --no-warnings run.ts",
    "agent": "node --experimental-strip-types --no-warnings run.ts",
    "agent:bun": "bun run run.ts"
  },
  "dependencies": {
    "@cursor/sdk": "^1.0.11",
    "dotenv": "^17.4.2",
    "openai": "^4.79.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "typescript": "^5.6.3"
  }
}
